fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2
python-dotenv==1.0.1
pydantic-settings==2.5.2
Faker==28.4.1
python-multipart==0.0.9
email-validator==2.2.0

# Day 4 — Embeddings & Skill Matrix
sentence-transformers==3.0.1
numpy==1.26.4

# Day 7 — Team Formation (clustering)
scikit-learn==1.5.2
scipy==1.14.1

# Day 11 — Explainable AI Layer (SHAP on the success-probability model)
shap==0.46.0

# Day 4-6 — test suite (repository/service unit tests + router integration tests)
pytest==8.3.3
httpx==0.27.2

# Day 17 — lightweight MLflow model-version tracking for the success-probability
# model. mlflow-skinny (not the full "mlflow" package) intentionally: no
# built-in tracking server/UI process, just the client API against a local
# file store (see app/config.py's MLFLOW_TRACKING_URI) — matches the spec's
# "lightweight" ask without adding a service to docker-compose.yml.
mlflow-skinny==2.17.2

# Gap-fix (post-Day-20) — Engineering Knowledge Graph. In-process graph
# library (no server), per the execution guide's original Day-1 tech
# choice — this was pinned in the plan from Day 1 but never actually
# added to requirements.txt or built until this pass.
networkx==3.3
